{
  "network": {
    "servers": [ "logging.default.svc.cluster.local:5000" ],
    "ssl ca": "/etc/logstash/ssl/lumberjack.crt",
    "timeout": 15
  },
  "files": [ {
    "paths": [
      "/tmp/log/*log"
    ],
    "fields": {"type": "logs","application":"server"}
  }
  ]
}
